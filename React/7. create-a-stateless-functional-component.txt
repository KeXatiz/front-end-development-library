// note:  stateless component as one that can receive data and render it, but does not manage or track changes to that data.

const MyComponent = function() {
  // Change code below this line
  return(
    <div> <p>hello world </p> </div>
  );


  // Change code above this line
}